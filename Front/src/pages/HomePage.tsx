import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
import { ReactElement, useState } from "react";
import Button from "../UI/Button";
import Alert from "../UI/Alert";

export default function HomePage(): ReactElement {
  const [popup, setPopup] = useState<boolean>(false);
  const [key, setKey] = useState(0);

  function handleclick() {
    setPopup(true);
    setKey((prevKey: number) => prevKey + 1);
  }
  return (
    <div className="px-10">
      {popup && (
        <Alert key={key}>
          Vous avez cliquer sur le bouton inscription Lorem ipsum dolor sit.
        </Alert>
      )}
      <h2 className="text-2xl text-blue-400">Homepage</h2>
      <Button variant="border" onClick={handleclick}>
        Toaster !
      </Button>
    </div>
  );
}
